/**
 * Create a new user.
 * @param {Object} options
 * @throws {Error}
 * @return {Promise}
 */

module.exports = (options) => {
  return new Promise((resolve, reject) => {
    resolve({ text: 'ok!' })
  })
}
